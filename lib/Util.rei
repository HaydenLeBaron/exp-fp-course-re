/** Returns a colorful "Hello, World" message.

    {4 Examples}

    {[
    print_endline(hello());
    ]} */

let id: 'a => 'a;

let assertInt: (string, int, int) => unit;

// Create function to print a list
let show: ('a => string, Lst.lst('a)) => string;

let show: ('a => string, Lst.lst('a)) => string;

let showInt: Lst.lst(int) => string;
// Why is 'id' used?
let showString: Lst.lst(string) => string;
